<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Books" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <!-- Basic Needs -->
    <nav class="top-bar">
      <ul>
        <li class="name"><h1><a href="/">Books</a></h1></li>
      </ul>
    </nav>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>